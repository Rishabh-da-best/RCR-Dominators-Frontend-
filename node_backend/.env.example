# Copy this file to .env and fill in real values before deploying.
# .env is excluded from Docker and git — never commit real secrets.

NODE_ENV=production
SESSION_SECRET=replace-with-a-long-random-string
